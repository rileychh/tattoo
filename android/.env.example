# Path to Android SDK
ANDROID_HOME=/path/to/android/sdk

# Secret key for encrypting/decrypting keystore credentials
# Set during first run of match_keystore plugin or obtain from your team
MATCH_KEYSTORE_SECRET=

# Git repository URL for keystore storage (private repo)
MATCH_KEYSTORE_GIT_URL=
